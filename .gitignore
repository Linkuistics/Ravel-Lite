# Pi session storage (created at runtime by ravel-lite)
.pi/
*.session.jsonl
# OS
.DS_Store
/target
# Playwright MCP scratch dir (created when a work phase uses the
# browser tools to visually verify built HTML)
.playwright-mcp/
