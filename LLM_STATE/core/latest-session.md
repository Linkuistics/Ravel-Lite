### Session 1 (2026-04-18T04:29:42Z) — tighten backlog status tracking and move source-commit to analyse-work

- **Attempted:** Two tasks from the backlog in a single session: (1) make backlog `Status:` transitions reliable, and (2) move source-commit authority from the work phase to analyse-work.
- **What worked:**
  - `defaults/phases/work.md` step 7 rewritten so the `Status:` flip is named as the *first* required sub-action before writing a `Results:` block, with an inline rationale.
  - `defaults/phases/work.md` step 8 inverted: work phase told explicitly *not* to commit source files and to leave the tree dirty for analyse-work.
  - `defaults/phases/analyse-work.md` extended with a `{{WORK_TREE_STATUS}}` section near the top, a new step 5 (safety-net status flip) and step 6 (stage + commit every path outside the plan dir, or justify skipping each path in the session log).
  - `src/git.rs`: `work_tree_snapshot(project_dir, baseline_sha)` added — composes a labelled `git diff --stat <sha>` + `git status --porcelain` block; soft-fails on git errors so it can never wedge `compose_prompt`. Two unit tests cover dirty-tree and clean-tree cases.
  - `src/phase_loop.rs`: when entering `LlmPhase::AnalyseWork`, the loop reads `work-baseline`, calls `work_tree_snapshot`, inserts `WORK_TREE_STATUS` into a cloned token map, and composes the prompt from the augmented map. Captured at prompt-compose time so hand-edits made between work exit and analyse-work start are included.
  - `tests/integration.rs`: `ContractMockAgent` extended with `captured_prompts` (for token-substitution assertions) and opt-in `commit_project_dir`. New test `analyse_work_flips_stale_task_status_per_safety_net` pre-seeds a stale-status backlog task, runs the phase loop, declines the reflect-phase confirm, and asserts `Status: done`. New test `analyse_work_receives_snapshot_and_commits_uncommitted_source` verifies prompt substitution and mock source-commit. All 140 tests (132 lib + 8 integration) pass.
- **What didn't:** The `defaults/phases/analyse-work.md` step numbering was initially left with two adjacent "7." entries after the insertion; fixed in a second edit pass. The work phase in this very session ran under the *old* work.md (which said to commit), so the source changes landed in two separate commits (3621738, 98e9ae9) before analyse-work ran — this is a one-time transition artifact; future sessions will use the new hand-off contract.
- **What this suggests trying next:** Monitor whether the prompt-level status-flip instruction in the tightened work.md closes the gap in practice before relying on the analyse-work safety-net. If LLMs routinely justify away source commits, escalate to a script-phase `git-commit-source` that unconditionally commits every non-plan path.
