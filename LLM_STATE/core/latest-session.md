### Session 2 (2026-04-21T05:06:17Z) — Terminal-title OSC on phase transitions

- **Attempted:** Implement terminal-title updates via OSC escape sequences at every phase transition and push/pop site in `phase_loop.rs`, plus a new `src/term_title.rs` module.
- **What worked:** Fully implemented as specified. New `src/term_title.rs` exposes `set_title(project, plan, phase)` and a testable `format_title_escape` helper with tmux passthrough (doubled inner ESCs). Call sites wired at: `phase_loop` LlmPhase entry, `run_stack` LlmPhase entry, `do_push` after `sync_stack_to_disk`, and both pop sites in `run_stack`. Phase labels are uppercased (e.g. `ANALYSE-WORK`) matching the existing phase-header banner convention. 44 existing integration tests pass; 6 new `term_title` unit tests pass; `cargo clippy --all-targets` clean. Ratatui's stderr backend means stdout OSC writes are a clean side-channel.
- **What didn't work:** Nothing — implementation was straightforward. The `raw_phase_label` helper was added to avoid panicking at push/pop sites where `LlmPhase` isn't yet known.
- **What to try next:** The other open backlog items are independent: coordinator-plan decomposition in `create-plan.md`, hand-off rationale preservation across analyse-work/triage, narrowing `warn_if_project_tree_dirty`, and the `--debug-file` workaround removal.
- **Key learnings:** `do_push` returned `sync_stack_to_disk(...)` directly before this change; adding the title call required splitting the `?` propagation and explicitly returning `Ok(())`. The OSC noise in `cargo test` output is harmless but could be gated on `stdout().is_terminal()` in future if it becomes annoying.
