### Session 1 (2026-04-18T05:56:40Z) — Fix token substitution, version CLI, pi stderr capture

- Attempted 5 tasks from the backlog: substitute_tokens ordering fix, version subcommand, pi stderr capture, pi model bump, and provider assertion in embedded_defaults_are_valid test.
- All 5 tasks completed successfully. `substitute_tokens` reordered to expand RELATED_PLANS (and custom tokens) before path tokens, fixing fatal errors on all three plans whose related-plans.md used {{DEV_ROOT}}. The `raveloop version` subcommand and --version/-V flags wired via clap-derive. PiAgent::invoke_headless ported to pipe stderr into a 4096-byte rolling buffer, eliminating TUI bleed-through and surfacing the tail in error messages on failure. Pi work model bumped from claude-opus-4-7 to match claude-code defaults. `embedded_defaults_are_valid` extended to assert provider is non-empty in pi's embedded config.
- Full test suite (144 unit + 8 integration) stayed green after each task. Duplicate backlog entries for "Bump pi default models" and "Extend embedded_defaults_are_valid" remain as not_started — these are stale triage artefacts for tasks already completed above; triage should prune them.
- Key learning: the two-tier substitution ordering (content macros first, then atomic path tokens) must be documented in-code so future refactors don't silently reintroduce the bug — done via comment. Also noted: the STDERR_BUFFER_CAP constant and warning_line helper are now duplicated across pi.rs and claude_code.rs, with explicit comments pointing to the extraction task (task 7) that will unify them.
