id: session-138
timestamp: 2026-05-03T23:41:34Z
phase: work
body: |
  ### Session 138 (2026-05-03T23:41:34Z) — Complete ErrorCode tagging sweep across six remaining source files

  - Completed the final leg of the project-wide `ErrorCode` tagging sweep, covering the 6 files deferred from prior cycles: `config.rs`, `create.rs`, `git.rs`, `multi_plan.rs`, `plan_inspect.rs`, `config_lua.rs`.
  - 22 macro sites tagged: 2 in `config.rs`, 12 in `create.rs`, 2 in `git.rs`, 7 in `multi_plan.rs`, 4 in `plan_inspect.rs`, 5 in `config_lua.rs`.
  - All `bail!`-at-statement-position sites swapped 1:1 to `bail_with!(ErrorCode::X, …)`; `anyhow!` inside `.ok_or_else`/`.map_err` chains kept the macro and chained `.with_code(ErrorCode::X)` afterwards, matching existing convention. `plan_inspect.rs` status-parser fns restructured to `match` + `bail_with!` for readability.
  - `with_context` chains intentionally left untouched — out of scope; tagged `bail!`/`anyhow!`/`ensure!` only.
  - `./scripts/check.sh` clean; `cargo test --workspace` all green (999 tests in the main crate, no regressions).
  - Two natural follow-ups identified: a grep-based regression guard in `scripts/check.sh` to prevent new untagged sites drifting in, and a `with_context` IO-chain sweep to close the last gap before JSON-error-envelope work.
