id: session-145
timestamp: 2026-05-04T07:51:16Z
phase: work
body: |
  ### Session 145 (2026-05-04T07:51:16Z) — Add `legacy` field to BacklogEntry and three migrate LlmPhase variants

  - **What was attempted:** Phase 1 of the migrate-v1-v2 plan: two parallel tasks — add `legacy: bool` to `BacklogEntry`, and add `MigrateIntent`, `MigrateTargets`, `MigrateMemoryBackfill` to `LlmPhase`.
  - **What worked:** Both tasks landed cleanly. `BacklogEntry.legacy` uses `#[serde(default, skip_serializing_if = "std::ops::Not::not")]` so the field is invisible in YAML when false; 14 `BacklogEntry { ... }` literal sites across tests and production code were patched. The three `LlmPhase` variants gained `as_str`/`parse` round-trips; non-exhaustive match sites in `format.rs`, `prompt.rs`, `phase_loop.rs`, and `tests/phase_loop.rs` were all handled. The migrate variants return `Ok(String::new())` from `load_phase_file` (P5 wires real prompts), and `unreachable!()` in both the production phase loop and the test mock. Full test suite green (1013 lib + 9 phase_loop + integration suites); clippy clean.
  - **What didn't work / gotchas:** Rustc emitted a spurious "unused variable: `prompt`" secondary error alongside the non-exhaustive match error in the test mock — adding the new arm cleared both. Future P5/P6 subagents touching match sites should watch for the same cascade.
  - **What this suggests trying next:** P2 (v2 path-shape gate) and P3 (mechanical migration half) are both fully unblocked and independent. P5 (three headless prompt files) is now unblocked by this session's placeholder arms in `prompt.rs::load_phase_file` (tagged `TODO(migrate-v1-v2 Phase 5)`). P4 (apply layers) still waits on P3's `Validated` struct.
