id: session-21
timestamp: 2026-04-22T23:47:28Z
phase: work
body: |
  ### Session 21 (2026-04-22T23:47:28Z) — Remove --debug-file TUI workaround

  - Attempted to remove the `--debug-file /tmp/claude-debug.log` workaround from `src/agent/claude_code.rs::invoke_interactive` now that the installed `claude` binary is 2.1.117 (past the 2.1.116 threshold specified in the task).
  - Deleted 27 lines: two `args.push` lines and the 24-line WORKAROUND comment block (lines 181–206 in the pre-change file). Forensic detail in that comment is preserved in git blame; recoverable via `git log -S "WORKAROUND" -- src/agent/claude_code.rs`.
  - `cargo check` and `cargo test` (406 lib tests + integration suites) passed cleanly. No test pinned the `--debug-file` args.
  - Task intentionally left as `blocked` rather than `done`: the task description is explicit that "a version bump alone is insufficient" — the fix is empirical and requires a human watching a real Work-phase TUI rendered by a freshly-built `ravel-lite` binary without the flag.
  - Next step: human rebuilds (`cargo install --path .`) and runs `ravel-lite run` against a real plan. If TUI renders normally → mark task done. If TUI silently fails → revert commit and re-block with note "2.1.117 still reproduces".
