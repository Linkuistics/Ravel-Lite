id: session-143
timestamp: 2026-05-04T04:40:35Z
phase: work
body: |
  ### Session 143 (2026-05-04T04:40:35Z) — Remove subagent-dispatch and wire findings inbox

  - Completed the remaining scope items for the findings-inbox task: source-side removal of the v1 `subagent-dispatch.yaml` mechanism and prompt rewrites in `triage.md`/`reflect.md`.
  - The schema/CLI/yaml-io layer and survey integration were already in place from a prior cycle; this session closed out the code and prompt side.
  - All tests pass (1100+); clippy clean.

  What worked:
  - Deleting `src/subagent.rs` and tracing all its callers upstream (`phase_loop.rs`, `agent/mod.rs`, `agent/pi.rs`, `agent/claude_code.rs`, `agent/common.rs`) was clean — no other callers existed.
  - `triage.md` replacement of the `[DISPATCH]` / `subagent-dispatch.yaml` section with a `ravel-lite findings add` block is a direct structural swap with no ambiguity.
  - `reflect.md` gained a new step 4 ("Cross-plan findings") and the old step 4 ("Hand off") was renumbered to step 5.

  What this suggests trying next:
  - The seven AsciiDoc website docs still describe `subagent-dispatch.yaml`; a docs-reconciliation pass is the natural follow-up.
  - Reflect's `state memory check-anchors` run should surface the now-stale `pi_dispatch_subagent` memory entry next cycle.
