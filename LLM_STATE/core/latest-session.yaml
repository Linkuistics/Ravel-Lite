id: session-83
timestamp: 2026-04-28T12:11:30Z
phase: work
body: |
  ### Session 83 (2026-04-28T12:11:30Z) — Decompose architecture-next umbrella into 13 sub-tasks

  - Worked the `begin-architecture-next-migration` umbrella task: closed it by decomposing into 13 trackable sub-tasks under category `architecture-next` and recording four settled Phase A decisions in `docs/architecture-next.md`.
  - All four open questions that had concrete answers were resolved: crate name and home (`knowledge-graph`, in-tree workspace member, extraction deferred post-v2.0), Datalog engine (`ascent` — recompute is fast enough at ravel-lite scale; differential-dataflow's incremental complexity is unwarranted), v1→v2 cutover (hard cutover; v2 binary refuses unmigrated plans), legacy backfill (one-shot LLM-driven extraction at migrate time; unattributable entries land with `status: legacy`).
  - `docs/architecture-next.md` updated: new *Settled decisions* section, Library extraction section revised to name the crate and state the in-tree-first policy. Remaining open questions left intact.
  - `default-config-location-...` flipped to `blocked` — its substance is subsumed by `ravel-context-new-layout-and-repos-yaml-registry`, which owns config discovery for v2.
  - No code written. Cargo workspace conversion, crate skeleton, and phase-loop changes deferred to sub-tasks.
  - The two foundation sub-tasks (`knowledge-graph-crate-scaffold-tms-kg-substrate`, `ravel-context-new-layout-and-repos-yaml-registry`) have no dependencies and can start in either order; both underpin five or six downstream tasks respectively. Triage should pick based on which is more disruptive to defer.
