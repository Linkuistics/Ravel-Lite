id: session-38
timestamp: 2026-04-24T05:42:52Z
phase: work
body: |
  ### Session 38 (2026-04-24T05:42:52Z) — Eliminate binary/library double-compilation

  - Executed task `eliminate-binary-library-double-compilation-in-main-rs`: converted `src/main.rs` from a parallel crate root into a thin consumer of the library crate.
  - Deleted the 20-line `mod xxx;` block from `src/main.rs`, replaced all `crate::` imports with `ravel_lite::` (20 call-sites), and added `use ravel_lite::{create, init, multi_plan, phase_loop, projects, related_components, state, survey};` for bare module paths.
  - Removed five `#[allow(unused_imports)]` attributes and their 6-line explanatory comment from `src/survey.rs`; removed three `#[allow(unused_imports)]` attributes and their 7-line comment from `src/ontology/mod.rs`.
  - All verification passed: `cargo build` clean, `cargo clippy --all-targets --all-features` clean under `deny(warnings)`, `cargo test` 541 tests pass, `cargo build -vv` confirmed exactly two `rustc` invocations (one lib, one bin), `grep '#\[allow(unused_imports)\]' src` yields 0 matches.
  - The pattern is now clean: the propagating `#[allow(unused_imports)]` debt on pub-use blocks is fully eliminated; future pub-use additions in library surface need no per-item workaround.
