id: session-17
timestamp: 2026-04-22T11:26:35Z
phase: work
body: |
  ### Session 17 (2026-04-22T11:26:35Z) — Delete legacy .md plan-state files via state migrate --delete-originals

  - Ran `ravel-lite state migrate --force --delete-originals LLM_STATE/core` after upgrading the installed binary to the R8 build. `--force` was required because the pre-existing `.yaml` files were stale relative to the authoritative `.md` files; re-deriving from `.md` and overwriting was the correct disposition.
  - Manually deleted `related-plans.md` (content was `_No active sibling plans._` — zero edges to preserve). `read_related_plans_markdown` returns `""` gracefully on absence, so the `{{RELATED_PLANS}}` token in future prompts substitutes to an empty block.
  - Verified via `ravel-lite state backlog list LLM_STATE/core` — expected tasks visible with round-trip content identical to pre-migration `.md`.
  - No source files changed; all diff is plan-state bookkeeping (`.md` deletions, `.yaml` refreshes).
  - What this suggests next: R7 implementation tasks (r7-1 through r7-11) are the next logical body of work; the `--delete-originals` maintenance task was the last dependency-clearing step before R7 execution.
